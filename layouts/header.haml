%header.flybar
  %hgroup
    %h1.title= "#{@item[:title]}"
    -if @item[:kind] == 'article'
      %h3.byline
        by 
        %a{:href => "http://ecarmi.org/"}
          Evan Carmi
      %span.post_date
        Posted
        %time.timeago{:datetime => item[:timeago]}
          = item[:print_time]
    /
      %h2.logo
        <span class="ecarmi">ecarmi</span><span class="dot">.</span><span class="tld">org</span>

  %nav.breadcrumbs
    %ul
      - breadcrumbs_trail.each do |crumb|
        %li
          =link_to(crumb.path.split('/').last || 'ecarmi.org', crumb)

