%section#articles
  - sorted_articles.each do |item|
    %article
      %div.post-content
        %h2.title= link_to item[:title], item

        %p.summary= item[:summary]

      %span.post-date
        %time.timeago{:datetime => item[:created_time]}
          = item[:print_time]
